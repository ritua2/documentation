To load the perl module for perl/5.26.3 migrate to a compute node then load the 
module via the command:

       module load perl/5.26.3

To test it with a simple test, run the attached test.pl script:

   perl test.pl

The expected output should be:

Hello, World!
Perl version : 5.026003


