To load the tcl module migrate to a compute node the load the interpreter
via the command:

    module load tcl


TEST: to test the interpreter execute the enclosed script
to print 'hello world to the screen via this command:

tclsh hello_world.tcl

The script output should be:

hello world



